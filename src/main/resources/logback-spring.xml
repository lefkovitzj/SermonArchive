<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Use Spring Boot's default logging properties and console/file configuration basics -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml" />

    <!-- ========================================== -->
    <!-- 1. LOCAL ENVIRONMENT PROFILE               -->
    <!-- ========================================== -->
    <springProfile name="local">
        <!-- Logs plain text to a rolling file for easy human reading -->
        <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>logs/app.log</file>
            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <fileNamePattern>logs/app-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
                <maxFileSize>10MB</maxFileSize>
                <maxHistory>7</maxHistory>
            </rollingPolicy>
            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] [TraceID: %X{traceId}] [Client: %X{clientIp}] %logger{36} : %X{httpMethod} %X{requestUrl} [%X{statusCode}] -> %msg%n</pattern>
            </encoder>
        </appender>

        <root level="INFO">
            <appender-ref ref="FILE" />
        </root>
    </springProfile>

    <!-- ========================================== -->
    <!-- 2. AWS / CLOUDWATCH ENVIRONMENT PROFILE    -->
    <!-- ========================================== -->
    <springProfile name="aws">
        <!-- CloudWatch handles text, but JSON allows you to use CloudWatch Logs Insights to query logs efficiently -->
        <appender name="JSON_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="ch.qos.logback.classic.encoder.JsonEncoder" />
        </appender>

        <root level="INFO">
            <appender-ref ref="JSON_CONSOLE" />
        </root>
    </springProfile>
</configuration>